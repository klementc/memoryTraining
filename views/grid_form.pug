extends layout

block content
  div
    h1 Grid

    button(type="button" class="float-right btn btn-info btn-lg" data-toggle="modal" data-target="#myModal") Rules

    div(id="myModal" class="modal fade" role="dialog" tabindex="-1" aria-labelledby="exampleModalLongTitle" aria-hidden="true")
      div(class="modal-dialog" role="document")
        div(class="modal-content")
          div(class="modal-header")
            button(type="button" class="close" data-dismiss="modal") &times;
            h4(class="modal-title") Rules
          div(class="modal-body")
            | This game consists in remembering which cells of a grid are activated.
            br
            | You can choose the size of the grid, and the duration of your memorization.
            br
            | When you click on submit, the grid appears. Your task is to remember the position of colored cells.
            br
            | Once time is up, click on the cells that you remembered, and validate. Your score is equal to the number of cells that were selected correctly and the number of cells that weren't activated, and not clicked.
          div(class="modal-footer")
            button(type="button" class="btn btn-default" data-dismiss="modal") Close
  hr

  form(method='POST' action='')
    div.form-group
      label(for='width') Width of grid:
      input#width.form-control(type='number', placeholder='5', name='width', value=(undefined===width ? '5' : width.name))

      label(for='height') Height of grid:
      input#height.form-control(type='number', placeholder='5', name='height', value=(undefined===height ? '5' : height.name))

      label(for='duration') Duration (sec):
      input#duration.form-control(type='number', step="any", placeholder='30', name='duration', value=(undefined===duration ? '30' : duration.name))

    button.btn.btn-primary(type='submit') Submit

  if errors 
   ul
    for error in errors
     li!= error.msg
